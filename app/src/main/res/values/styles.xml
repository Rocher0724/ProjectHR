<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--로그인 화면 edittext 스타일 지정 -->
    <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="colorControlNormal">@color/textmarker</item>
        <!--loginTextInputLayout-->
        <!-- 텍스트 선택시 물방울로 위치 표시해주는 마커의 색 -->
        <item name="colorControlActivated">@color/textmarker</item>
        <item name="android:textCursorDrawable">@color/loginTextInputLayout</item>
        <item name="android:textColor">@color/loginTextInputLayout</item>
        <item name="android:textColorHint">@color/loginTextInputLayout</item>
        <item name="android:textColorHighlight">@color/textHighLight</item>
        <item name="android:textSelectHandle">@color/textmarker</item>
        <item name="android:background">@null</item>
    </style>
    <!--텍스트 인풋 레이아웃 스타일 지정 -->
    <style name="EditTextInputStyle" parent="Widget.Design.TextInputLayout">
        <item name="hintTextAppearance">@style/TextAppearance.Design.Hint</item>
        <item name="errorTextAppearance">@style/TextAppearance.Design.Error</item>
        <item name="counterTextAppearance">@style/TextAppearance.Design.Counter</item>
        <item name="counterOverflowTextAppearance">@style/TextAppearance.Design.Counter.Overflow</item>
        <item name="passwordToggleDrawable">@drawable/design_password_eye</item>
        <item name="passwordToggleTint">@color/textmarker</item>
        <item name="passwordToggleContentDescription">@string/password_toggle_content_description
        </item>
    </style>

    <!--progress bar 색깔 지정-->
    <style name="progressbarColor">
        <item name="colorAccent">@color/progressBarColor</item> <!-- Whatever color you want-->
    </style>
</resources>
